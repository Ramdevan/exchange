<% if current_user.nil?%>
	window.location.assign('/signin');
<% else %>
	$('.activities_currency_details').html("<%= j(render partial: 'shared/modals/activity_lending', locals: {al: @al}) %>")
	$("#transferActivitydModal").modal('show');
<% end %>
