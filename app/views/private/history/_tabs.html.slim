ul.nav.nav-tabs.history_tabs role='tablist'
  - tabs.each do |name, args|
    - css_class = name == active ? 'active' : ''
    li class='nav-item'  = link_to t(args[0]), args[1], class: css_class+' '+'nav-link'
